# 板材設計 JSON 互換性契約

制定: V4.07 / 2026-09-18

## 最優先の方針

過去に保存した設計は利用者の資産。追加機能の都合で基本図形の表示・保存ができなくなる変更を禁止する。アプリのバージョン番号とファイル仕様の番号は別物である。

## 安定した基本データ（core schema 1）

- `format: "itazai-design"`, `schemaVersion: 1`, `version: 1` を維持する。番号を機能追加ごとに増やさない。
- `parts` は**加工後の完成形のスナップショット**。加工履歴を再生しなければ表示できない形式に変更しない。
- 部品の `id`, `name`, `stock`, `x`, `y`, `w`, `h`, `used`, `pos`, `rot` の意味と単位を変えない。
- `x/y/w/h` は材料上のmm。`outline` と `holes` は部品ローカル座標の輪郭。省略した `outline` は長方形、`holes` は穴なし。`cancelled` は未使用領域で、形状を削除してはならない。
- 公開JSONの `coordinateSystem: "right-handed-z-up"` と `parts/references` の配置・回転の意味を維持する。旧来のタグなしファイルは従来の内部座標系として変換する。未知の座標系を推測して読まない。
- `machiningOperations.before/after` の配置・回転は既存の内部スナップショット表現を維持する。公開 `parts.pos/rot` と混同しない。将来統一する場合は明示的な移行処理が必要。
- 選択IDが見つからない場合は先頭部品に補正できる。基本形状・位置・寸法を推測して修復してはならない。

## 付加情報と機能縮退

`machiningOperations`（加工の編集履歴）、`references`（仮置き）、表示角度・倍率、設計メモ、寸法線の加工分類などは、完成部品を表示するための必須条件にしない。

V4.07では `compatibility.featureVersions` に加工履歴・仮置きの版 `1` を記録する。未記載は旧形式の版1とする。未知の機能版、未知の加工方法、不正な履歴や付加情報は実行せず、対象を無効化して読み込む。加工履歴は依存関係があるため、不正な場合は履歴全体を無効化する。形状の表示は `parts` を使う。無効化した情報は `extensions.itazaiRecovery` の `{key,value}` にそのまま保管し、再保存にも含める。自動的に再有効化しない。

- 未知の `extensions` は不透明な値として保持する。新規機能は衝突しない名前の下に `version` とデータを置く。
- 未知の最上位項目も復旧用情報として保管する。実行はしない。
- 復旧用データは座標変換しない原情報であり、そのまま現在の編集データとして使用しない。
- 読み込みで縮退した時は利用者に対象を知らせる。元ファイルも保管する。
- 未対応の基本schema、未知の座標系、破損した基本形状は読み込みを拒否し、現在の設計を変更しない。
- 旧アプリ自体は修正できないため、「新しいデータを任意の旧版で完全に編集できる」とは保証しない。**新しいアプリが過去の有効なファイルを読めること**を優先する。

## 将来の変更手順（必須）

1. この文書と `AGENTS.md` を先に読む。
2. 基本形状の既存項目の削除・改名・単位変更をしない。追加情報は省略可能にする。
3. 基本仕様の変更が不可避な場合、旧schemaを残す読み込み移行処理・回帰テスト・仕様書を同時に追加する。古いデータを書き換えて帳尻を合わせない。
4. 履歴の新しい加工種別にはバージョンと縮退処理を設ける。保存時には完成形のスナップショットも必ず書く。
5. `tests/json-compatibility.cjs` を変更後のHTMLに対して実行し、旧保存例・再保存・未知機能・破損データを確認する。
6. 新機能について、読込→表示→保存→再読込と、縮退時に元情報が残ることを追加検証する。配置・輪郭・穴・回転の維持を比較する。
7. 旧HTMLと旧JSONを上書きしない。GitHub公開は利用者が依頼した時だけ行う。

## 検証

Node.js と Playwright、Chromeを使用する。

```sh
node tests/json-compatibility.cjs 板材設計_V4.07.html
```

Playwrightが通常のモジュールパスにない場合は `NODE_PATH` を設定する。Chromeの場所は `CHROME_PATH` で指定できる。

このテストは同梱の旧保存例フォルダーがある場合、そのJSONも読み込み・再保存する。テスト用に生徒などの実データを新たに公開リポジトリへ追加しない。幾何の妥当性はアプリの `valid/profileValid` で検証し、JSONの型検証だけで代用しない。
