# 板材設計を変更する際の引き継ぎ事項

## JSON互換性を最優先にする

利用者の明示要望（2026-09-18）: 旧版で作った設計資産を、機能追加によって表示不能にしてはならない。

- 保存・読込・形状・加工履歴・仮置き・座標系の変更前に `JSON_COMPATIBILITY.md` を読むこと。
- 完成部品の `parts` を安定した表示用データとして保持する。追加機能の履歴なしで表示できる設計を守る。
- 未知・不正な付加情報は基本形状と分離し、無効化して原文を再保存可能に保管する。基本形状の破損を無視して表示しない。
- アプリ版とJSON schema版を混同しない。schema 1の既存項目の意味・座標系・単位を変えない。
- 関連変更時は `tests/json-compatibility.cjs <変更後HTML>` と変更固有の回帰確認を行う。未検証範囲を明記する。
- 既存の番号付きHTMLを残し、次の番号のHTMLを作る。旧設計JSONも上書きしない。
- GitHubへの公開は明示依頼の範囲で行う。
