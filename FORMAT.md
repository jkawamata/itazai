# 板材設計JSON形式

V2.67以降は、保存時に次のメタデータを付加します。

```json
{
  "format": "itazai-design",
  "schemaVersion": 1,
  "generator": { "id": "itazai", "name": "板材設計", "version": "2.67" },
  "savedAt": "2026-09-13T02:30:00.000Z",
  "version": 1,
  "reportReferences": "参考にしたものの記述",
  "reportViewAngles": [-0.7853981633974483, 0.6154797086703873],
  "reportViewZoom": 1,
  "coordinateSystem": "right-handed-z-up"
}
```

この例は項目の抜粋です。実際には `title`, `next`, `selected`, `kerf`, `parts`, `references` などの設計データも含みます。

## 集計側での識別

1. JSONを解析します。
2. `format === "itazai-design"` と `schemaVersion === 1` を確認します。
3. 設計の `version === 1`、`parts` が配列であること、各項目の型・範囲など、使用するデータの構造を検証します。
4. 対応しない形式・バージョンや壊れたファイルは集計対象から分けます。

メタデータは編集できるため、アプリ由来の形式を識別するための自己申告情報です。真正性や未改変の証明ではありません。旧バージョンのファイルには `format` がなく、形状の構造から互換ファイルとして読み込めますが、作成元を確定できません。新版で保存し直すと識別情報が付きます。

## レポート項目

- `reportReferences`：120 UTF-16コード単位以内の文字列。未入力は空文字列。
- `reportViewAngles`：構想図カメラの `[yaw, pitch]`。単位はラジアン。部品の回転角度とは別です。
- `reportViewZoom`：標準の自動フィットに対する倍率。0.5〜1.5、1が100%。省略された旧データは1として扱います。
- `savedAt`：保存時の端末時計によるUTC日時（ISO 8601）。サーバーによる証明日時ではありません。
- `parts[].machining`：加工方法・加工箇所の記録。旧設計にはない場合があります。

レポートを変更した後は設計ファイルを保存し直してください。角度・倍率を読み込み直すと構想図に反映し、印刷とPowerPoint出力にも使用します。

## 氏名等

学年・クラス・番号・氏名および保存ファイル名の専用項目はJSON本文に含めません。集計側ではファイル名 `学年クラス番号2桁_YYYYMMDD_HHMM_氏名.json` から取得してください。自由記入欄の文章はそのまま保存します。

`parts[].cancelled` が `true` の領域は取り消された未使用領域です。部品点数・加工集計から除外し、`used` は `false` です。省略時は通常の部品として扱います。


## 互換性の維持（V4.07以降）

仕様変更時の必須方針は [JSON_COMPATIBILITY.md](JSON_COMPATIBILITY.md) を参照してください。基本schemaは1を維持し、追加機能が使えない場合も完成形の部品スナップショットを読み込みます。
